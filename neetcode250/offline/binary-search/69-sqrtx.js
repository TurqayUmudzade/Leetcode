/**
 * 69. Sqrt(x)
 * https://leetcode.com/problems/sqrtx/
 *
 * Return the integer square root of x (floor).
 * Do not use built-in exponent/sqrt functions.
 * Time: O(log x)  Space: O(1)
 *
 * @param {number} x - non-negative integer
 * @return {number}
 */
var mySqrt = function(x) {};

// --- Tests ---
console.assert(mySqrt(0) === 0, 'Test 1 failed');
console.assert(mySqrt(4) === 2, 'Test 2 failed');
console.assert(mySqrt(8) === 2, 'Test 3 failed');
console.assert(mySqrt(9) === 3, 'Test 4 failed');
console.log('All tests passed (or stubs — implement the function)');
