/**
 * 43. Multiply Strings
 *
 * Given two non-negative integers num1 and num2 represented as strings,
 * return the product of num1 and num2, also represented as a string.
 * You must not use BigInt or convert the inputs to integers directly.
 * Simulate long multiplication using digit arrays.
 *
 * Examples:
 *   "2"   × "3"   → "6"
 *   "123" × "456" → "56088"
 *   "0"   × "52"  → "0"
 *
 * @param {string} num1
 * @param {string} num2
 * @return {string}
 */
var multiply = function(num1, num2) {

};

// Tests
console.assert(multiply("2", "3") === "6", "Test 1 failed");
console.assert(multiply("123", "456") === "56088", "Test 2 failed");
console.assert(multiply("0", "52") === "0", "Test 3 failed");
console.assert(multiply("99", "99") === "9801", "Test 4 failed");
